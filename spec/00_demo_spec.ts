describe('Writing a spec', () => {
    it('is sick nasty my man, good on you.', () => {
        //given
        const a = 10, b = 20;

        //when
        const answer = a + b;

        //then
        expect(answer).toBe(30);
    });
});